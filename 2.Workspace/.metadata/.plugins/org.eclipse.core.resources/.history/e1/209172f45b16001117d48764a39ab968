#ifndef INC_PROTOCOLTASK_H_
#define INC_PROTOCOLTASK_H_

#include <stdint.h>
#include <stdbool.h>
#include "FreeRTOS.h"
#include "queue.h"
#include "ProtocolDefinition.h"
#include "Configuration.h"

extern QueueHandle_t xQueue_ValidFrame;  /* Protocol → DeviceManager */

typedef struct{
    bool     waitingAck;
    uint8_t  txSeq;         /* SEQ we sent, expect same back  */
    uint8_t  retryCount;
    TxCommand_t pending;    /* copy of last sent command      */
}SlaveProtoState_t;

void Protocol_Init(void);
void Protocol_Task(void *pvParams);

#endif /* INC_PROTOCOLTASK_H_ */
